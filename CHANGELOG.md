# 更新日志

本文件记录 NAI Diffusion X 的版本变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [4.6.9] - 2026-09-01

### 修复

- **移除全局拖拽禁用**：删除 `document.dragstart.preventDefault()`，避免插件 WebView 将系统文件拖入误判为页面内部拖拽后被强制禁止。
- **恢复单一拖放路径**：删除后来叠加的 window/document/body 捕获监听、WeakSet 去重和行内 `ondragover`，画布拖入完整恢复为 `v4.5.8` 的 `#board dragover/drop -> uploadFiles()` 链路。
- **保留元素级禁拖**：画布图片和大图预览仅通过各自的 `draggable=false` 禁止向外拖出，不再影响外部文件拖入。
- **刷新画布缓存键**：静态资源切换到 `v=4.6.9`。

### 测试

- 全量测试通过：481 passed，203 subtests passed。

## [4.6.8] - 2026-09-01

### 修复

- **修复插件页禁止投放光标**：除 window/document 捕获监听外，为页面 body 增加原生 `ondragover` 兜底，为画布增加 `dropzone="copy"`，覆盖 AstrBot 宿主在模块事件之前接管默认拖放行为的情况。
- **保留角色提示词翻译**：角色正面/负面文本继续沿用提示词卡片翻译器。
- **更新画布资源缓存键**：静态资源切换到 `v=4.6.8`。

### 测试

- 画布页面测试及全量测试通过。

## [4.6.7] - 2026-09-01

### 新增

- **角色提示词翻译机制**：角色模块正面/负面输入沿用提示词卡片翻译器，中文角色描述会在提交生成前逐项翻译，并保留翻译失败时的原文回退。

### 修复

- **修复插件窗口拖放拦截**：除 document 捕获外，在 window 捕获阶段也接管画布文件 `dragenter/dragover`，覆盖宿主先于页面处理拖动事件的 WebView 分发路径；drop 仍通过 WeakSet 防止重复上传。
- **补充缓存刷新**：画布脚本资源切换到 `v=4.6.7`，确保插件页加载本轮拖放修复。

### 测试

- 全量测试通过：481 passed，203 subtests passed。

## [4.6.6] - 2026-09-01

### 新增

- **角色提示词翻译**：角色模块的正面与负面文本现在沿用提示词卡片的翻译器，逐项翻译中文角色描述；翻译失败时保留原文并写入调试记录。

### 修复

- **修复插件页外部拖入**：在 document 捕获阶段无条件接管画布范围内的 `dragenter/dragover`，并在 `drop` 时从 `DataTransfer.files` 或 `items.getAsFile()` 读取文件，兼容 AstrBot WebView 的宿主事件分发。
- **更新画布缓存键**：静态资源切换到 `v=4.6.6`，确保插件页加载最新拖放代码。

### 测试

- 全量测试通过：481 passed，203 subtests passed。

## [4.6.5] - 2026-09-01

### 修复

- **允许插件 WebView 接收外部文件**：在 document 捕获阶段同时处理文件 `dragenter` 与 `dragover` 并调用 `preventDefault()`，解决宿主把拖动目标报告为 `body/document` 时出现禁止投放光标、无法产生 `drop` 的问题。
- **修复坐标分区参数报错**：按坐标分区时保留 `use_coords=true` 和角色 `centers`，但不再发送部分网关拒绝的 `v4_prompt.use_order=false`；按出场顺序仍发送 `use_order=true`。
- **更新画布缓存键**：静态资源切换到 `v=4.6.5`，确保插件页加载最新拖放处理代码。

### 测试

- 全量测试通过，并新增坐标模式省略 `use_order=false`、顺序模式保留 `use_order=true` 的载荷测试。

## [4.6.4] - 2026-09-01

### 修复

- **修复 WebView 系统投放目标**：外部文件投放的捕获阶段兜底不再要求事件目标属于 `#board`，兼容宿主把目标报告为 `document/body` 的情况；投放坐标会限制在画布可视区域内。
- **恢复大图禁拖属性**：补回 `26287c3` 删除的 `draggable=false`，避免页面内部图片拖拽污染全局拖放状态。
- **刷新前端缓存键**：画布静态资源更新为 `v=4.6.4`，确保 WebView 加载本轮脚本。

### 测试

- 全量测试通过：480 passed，203 subtests passed。

## [4.6.3] - 2026-09-01

### 修复

- **定位拖放回归提交**：确认 `4.5.9 -> 4.6.0` 期间的 `c9e052a` 改写了 `DataTransfer` 判断、增加 `dragenter` 并在拖放事件中阻断传播；现已恢复 `4.5.9` 的板内事件链，并增加捕获阶段兜底。
- **强制刷新画布资源**：为 `canvas.css` 和 `canvas.js` 增加 `v=4.6.3` 缓存键，避免 AstrBot WebView 继续运行旧版拖放脚本。
- **固定右侧胶囊开关**：展开与收起状态都固定在屏幕右侧垂直中点，保持同一位置操作。
- **调整图片投影**：使用参考灯箱的 `0 24px 70px rgba(10, 15, 35, .42)` 投影，并确保外层不裁剪阴影。

### 测试

- 全量测试通过，并验证拖放事件代码与 `4.5.9` 的核心链路一致。

## [4.6.2] - 2026-09-01

### 修复

- **恢复 4.5.8 外部拖入链路**：按 `v4.5.8` 原样恢复 `dragover` / `drop` 处理逻辑，外部图片直接通过 `DataTransfer.files` 进入上传流程。
- **恢复信息栏胶囊开关**：展开时位于图片舞台与信息栏之间的垂直中点，收起后移动到右侧中点；移动端恢复到底部信息栏上沿中央。
- **修正大图投影**：投影重新跟随实际图片轮廓，并取消外层裁剪，避免阴影被截断或呈现为错误的整块矩形。

### 测试

- 全量测试通过，并通过浏览器几何检查验证胶囊位置与投影渲染层级。

## [4.6.1] - 2026-09-01

### 变更

- **优化大图预览控件**：左右导航按钮相对图片舞台保持一致间距；信息栏开关改为右上角独立圆形按钮，避免与导航按钮混淆。
- **规范外部导入名称**：外部导入图片显示为“导入图片 + 时间戳”，原始文件名保留在图片元数据中；信息栏长标题强制单行省略，不再撑出容器。
- **补充参考项目说明**：README 与第三方归属文档均注明大图预览布局参考 [AgIzT/NovelAI-Tag](https://github.com/AgIzT/NovelAI-Tag)。

### 修复

- **恢复外部文件拖放**：画布在 `dragenter/dragover` 阶段无条件允许浏览器继续文件拖放，并在 `drop` 时兼容从 `DataTransfer.files` 或 `DataTransfer.items.getAsFile()` 提取图片，修复部分 Windows WebView 无法导入的问题。

### 测试

- 全量测试通过：480 passed，203 subtests passed；浏览器几何检查确认左右导航间距一致、标题不会侵入信息栏开关。

## [4.6.0] - 2026-09-01

### 变更

- **复刻沉浸式大图预览**：参考 [AgIzT/NovelAI-Tag](https://github.com/AgIzT/NovelAI-Tag) 的灯箱设计，采用左侧居中原图、右侧参数信息栏、毛玻璃背景和响应式移动端信息面板。
- **完善预览浏览操作**：支持素材库及画布图片的上一张/下一张循环切换、键盘方向键切换，以及信息栏展开和收起。

### 修复

- **修复预览控件重叠**：增大下一张按钮与信息栏的间距，将信息栏展开/收起按钮固定在右侧，使两种状态下都能在同一位置操作。
- **修复预览图片定位**：图片在信息栏展开时居中于左侧可用区域，收起后居中于整个视口，不再因 `fit-content` 收缩而贴边。
- **恢复外部图片拖入画布**：兼容 Windows 资源管理器及不同 WebView 的 `DataTransfer` 文件表示方式，重新支持把外部 PNG、JPEG、WebP 和 GIF 直接拖入画布。

### 测试

- 画布页面与存储相关测试通过，并通过浏览器几何检查验证展开/收起位置。

## [4.5.9] - 2026-09-01

### 变更

- **重做画布大图预览**：采用浅色渐变背景、左侧大图与右侧信息卡的桌面布局，更接近图库式浏览体验；不增加左上角关闭按钮，继续支持按 `Esc` 或点击背景关闭。
- **完善预览信息卡**：增加图片标题、尺寸/比例/种子、正向 Tags、Negative、Note，以及复制全部、下载原图、反推此图和放入画布等操作。

### 测试

- 全量测试通过：480 passed，203 subtests passed。

## [4.5.8] - 2026-09-01

### 变更

- **提示词敏感词检测改为图片混淆触发器**：`prompt_block_enabled` 现在只检测敏感词并触发发送副本的本地马赛克混淆，不再过滤正向提示词或追加安全负面词。
- **移除视觉审核提供商配置**：QQ 防封流程不再依赖视觉审核模型，图片混淆在本地完成，不上传到外部网站；旧版视觉审核字段仍兼容读取但不参与主流程。
- **翻译结果同样参与检测**：中文提示词翻译后命中敏感标签时，也会触发本轮图片混淆。

### 新增

- **本地图片混淆模块**：新增与小番茄一致的 Gilbert Curve 像素置换，输出带 NovelAI 元数据的无损 PNG，混淆失败时不会发送原图。

### 修复

- **修复原始提示词元数据复用**：勾选「原始提示词」反推带 NovelAI 元数据的图片时，完整复用原图 prompt，不再只保留部分标签。
- **兼容旧画布缓存**：旧缓存缺少完整 prompt 时会自动重新读取图片元数据。
- **附加解混淆提示**：检测到敏感词时，在同一条图片消息下方显示“⚠️检测到nsfw内容，请自行解混淆”。

### 测试

- 全量测试通过：480 passed，203 subtests passed。

## [4.5.7] - 2026-09-01

### 变更

- **统一卡片倒角**：以角色模块卡为基准，将画布节点和附属卡片外层倒角统一为 `16px`，内层标题栏使用 `15px`。
- **限制提示词卡片尺寸**：提示词卡片最小尺寸调整为 `320×430px`，最大宽度限制为 `480px`，避免拉宽后角色模块过度增长。

### 修复

- **保护角色模块可见性**：角色模块展开时，调整提示词卡片宽度会根据画布顶部可视空间自动限制，避免角色模块被顶部裁切。
- **保持角色画布比例**：角色模块坐标预览始终保持 `1:1`，拉宽提示词卡片时高度同步增加，不再被压缩成横向区域。
- **修正角色删除图标对齐**：重置删除、确认和取消按钮的默认内边距、行高与外观，确保图标在统一按钮盒中居中显示。
- **移除角色画布分割线**：去掉角色模块坐标画布及内部裁剪框的白色边框，避免灰色遮罩与画幅之间产生视觉分割线。

### 测试

- 全量测试通过：476 passed，203 subtests passed。

## [4.5.6] - 2026-09-01

### 变更

- **固定方形角色画布**：角色位置板始终保持 `1:1`，当前提示词画幅以居中的有效裁剪框显示；竖图左右留出遮罩，横图上下留出遮罩，圆点、双击和拖动坐标均相对于裁剪框计算。
- **优化角色模块展开**：标题栏固定在提示词卡上方同一位置，正文只向上展开；画布位于顶部，角色编辑居中，按钮和操作提示置于底部，并移除 `V4+` 标记。
- **角色编辑器侧移**：角色卡改为当前选中角色的侧边浮层，角色模块主体不再被多个角色编辑卡撑高；屏幕边缘自动切换浮层方向。
- **优化角色编辑浮层尺寸**：移除空角色占位块，侧边角色卡宽度随提示词节点宽度变化，避免节点放大后编辑器仍固定在小尺寸。
- **优化角色布局模式**：按坐标分区与按出场顺序改为左右二选一开关，避免两个模式同时启用；旧存档会自动按坐标优先归一化。
- **优化角色编辑卡样式**：统一角色区域的边距、圆角和输入框高度，编辑卡默认宽度收窄，避免初始横向占用过长。
- **简化角色编辑浮层**：去掉侧边浮层的外层边框和背景，只保留角色卡本身；正面/负面输入框初始高度进一步提高。
- **移除角色卡蓝色投影**：选中角色时保留边框提示，但不再显示外围蓝色光晕。
- **统一角色卡字体**：角色名、坐标、字段标签和正负面输入文字跟随提示词卡片的字号层级，保持阅读比例一致。
- **统一附属卡片视觉层级**：移除角色/高级参数/原图标签的额外彩色选中边框，内部区域保持中性色；统一卡片圆角和间距。
- **降低角色编辑误触**：删除按钮首次点击进入确认状态，左侧显示确认按钮，原位置变为取消按钮。
- **修正附属卡片状态**：撤销容易造成内外框叠色的彩色选中边框，并移除高级参数与原图标签卡的默认投影。
- **修正角色删除确认**：修复确认按钮默认可见、取消按钮图标缺失导致的错误状态。

### 修复

- **修复采样器拒绝**：当中转网关不支持原图或手动选择的采样器时，自动回退到插件默认采样器后重试，避免 `Unsupported sampler` 直接失败。
- **修复大图预览布局**：Tags 区改为填充侧栏剩余高度，移除高级参数标题的遮挡式长提示。
- **修复加权 prompt 分词**：兼容连续权重前缀和缺失分隔符的原图元数据，避免清洗时吞掉后续提示词。

### 测试

- 全量测试通过：476 passed，203 subtests passed。

## [4.5.5] - 2026-08-31

### 新增

- **角色画布双击添加**：角色模块移除标题栏添加按钮，双击位置板即可在对应坐标创建角色圆点。
- **采样器自主切换**：高级参数新增采样器列表，支持跟随原图/默认值或按节点手动选择；补充 DPM++ 2M SDE 等采样器支持。

### 修复

- **修复角色画布画幅刷新**：修改提示词卡片画幅后，角色位置板立即同步新的横竖比例，并限制竖图预览高度。
- **修复原图 prompt 误删**：仅移除明确的画师/质量控制词，保留合法视觉标签、重复标签和加权标签。
- **修复原图标签卡截断**：标签卡高度改为按视口可用空间适配，避免有剩余空间时过早出现滚动条。
- **修复画布版本显示**：统一插件元数据与后端画布版本为 4.5.5。

### 测试

- 全量测试通过：476 passed，203 subtests passed。

## [4.5.4] - 2026-08-31

### 新增

- **常驻角色模块**：角色模块固定显示在每个提示词节点上方，无需连接原图即可创建并发送结构化多角色参数。
- **角色增删与点选编辑**：标题栏可添加角色，编辑器可删除角色；点击位置圆点会切换到对应角色的正面/负面编辑器。
- **随机位置布局**：新增带边缘与最小间距约束的随机位置，仅重排启用角色并保留禁用角色坐标。

### 变更

- **纯色角色位置板**：移除原图缩略图底图，改为跟随画幅比例的纯色位置板；拖动圆点仍保留精确 `center` 坐标。
- **角色模块独立于反推**：普通提示词生成同样发送启用角色；断开或更换原图不会清除用户手动创建的角色。
- **简化角色编辑器**：移除可见 X/Y 数字输入，只保留圆点拖动定位；“角色负面”简化为“负面”。

### 修复

- **修复提示词卡滚轮失效**：只有实际聚焦的输入框接管滚轮；仅选中或悬停提示词卡时，滚轮继续缩放画布。
- **修复滚动卡片边界锁定**：原图标签与角色正文滚动到顶/底后，继续滚动会把滚轮交还画布。
- **修复空角色与无标签渲染**：带有效位置的空白新角色可持久化但不会发送给 NovelAI；无原图标签时角色模块不再被误当成 DOM 节点导致渲染失败。

### 测试

- 补充常驻角色模块、空白角色持久化、添加/删除、点选编辑、随机位置、纯色位置板和滚轮行为回归检查。
- 全量测试通过：476 passed，203 subtests passed。

## [4.5.3] - 2026-08-31

### 新增

- **角色参数独立卡片**：角色控制从原图标签卡中拆出，独立显示在其下方，便于集中管理多角色参数。
- **角色位置联动预览**：新增原图缩略图和角色中心点标记，拖动编号标记即可修改角色的精确 X/Y 坐标。
- **角色快捷布局**：新增均匀横向、三人构图和四人构图快捷布局，应用后自动启用按坐标分区。

### 修复

- **优化原图标签滚动条**：补齐 Chromium/WebKit 和 Firefox 的细滚动条样式，统一轨道、滑块和悬停状态，避免显示系统原生滚动条。
- **完善角色参数持久化**：角色编辑、禁用状态、布局开关和卡片展开状态会随工作区保存，并支持恢复原图参数。

### 测试

- 补充角色独立卡片、缩略图拖拽、快捷布局、滚动条样式和工作区持久化回归检查。
- 全量测试通过：475 passed，203 subtests passed。

## [4.5.2] - 2026-08-31

### 新增

- **原图标签角色参数控制**：新增角色参数面板，可单独启用/禁用角色、编辑角色正面与负面提示词、修改精确 X/Y 坐标，并切换按坐标分区和按出场顺序。
- **恢复原图角色参数**：支持一键恢复原始角色提示词、精确坐标、启用状态和布局开关。

### 修复

- **修复角色参数缓存错位**：禁用角色改为按索引过滤，不破坏原始角色顺序以及正负 `char_captions` 的对应关系。
- **修复精确坐标被误判为网格站位**：带有 `center` 的角色不会再被自动推导的 `B3/C3/D3` 当作用户手动选定的位置。
- **修复官方响应体捕获超时**：官方接口返回完整 ZIP 但连接未及时发送 EOF 时，插件现在会识别完整文件尾并立即处理，不再无意义等待 180 秒。

### 测试

- 新增角色参数编辑、禁用角色、恢复原图参数、工作区持久化和官方 ZIP 无 EOF 响应测试。
- 全量测试通过：475 passed，203 subtests passed。

## [4.5.1] - 2026-08-31

### 修复

- **修复官方画布多角色位置异常**：`retagUseCoords=false` 现在作为权威值保留，不会被由精确中心点自动推导出的 `B3/C3/D3` 兼容站位错误覆盖为 `true`。
- **修复官方 prompt 重复注入角色标签**：使用 NovelAI 官方接口时，角色提示词只通过结构化 `char_captions` 发送，不再重复拼接到基础 prompt，避免重复标签干扰构图。
- **修复画布缓存中的站位误判**：带有精确 `center` 的角色条目不再被前端误认为用户手动选择了网格位置；旧版客户端仍保留显式 `position` 的兼容推断。

### 测试

- 补充官方 `use_coords` 语义和前端角色坐标缓存回归检查。
- 全量测试通过：472 passed，203 subtests passed。

## [4.5.0] - 2026-08-31

### 修复

- **修复 NovelAI V4+ 多角色 `char_captions` 坐标被量化**：保留官方元数据中的精确 `centers` 坐标，不再把 `0.202/0.453` 等坐标错误转换成 `0.3/0.5` 网格中心。
- **补齐多角色官方载荷**：正面与负面 `char_captions` 保持同序等长，并同步生成官方 `characterPrompts`，确保角色提示词、负面提示词和坐标不会错位。
- **保留跨接口兼容性**：中转网关继续使用 `characters` / `position` 格式，同时补发原生 NovelAI V4+ 字段；不支持结构化角色字段的网关仍可回退为普通提示词生成。
- **修复画布和图片反推的角色参数丢失**：精确坐标会在元数据解析、QQ 反推、画布缓存、工作区存档和再次生成之间完整传递。
- **修复旧画布存档的 `use_order` 默认值**：缺少该字段的旧存档现在正确按 NovelAI 默认值 `true` 处理。

### 测试

- 新增精确角色坐标、正负角色提示词对齐、画布缓存回写和旧存档兼容回归测试。
- 全量测试通过：472 passed，203 subtests passed。

## [4.4.9] - 2026-08-30

### 修复

- **下拉浮层按提示词卡片比例缩放**：浮层现在根据所在提示词卡片的实际渲染比例同步宽度、字号和选项高度，不再直接依赖全局画布缩放状态。
- **恢复下拉浮层原有长度**：浮层最大高度恢复为此前的 280px 规格，长列表仍可通过滚动条浏览。

## [4.4.8] - 2026-08-30

### 修复

- **长下拉框预选不再跳动**：鼠标移动到只露出一部分的选项时不再强制滚动整个浮层；只有选项完全离开可视区域后才调整浮层自身位置，避免画师列表预选时瞬间“弹一下”。

## [4.4.7] - 2026-08-30

### 修复

- **原图标签卡按内容溢出决定滚轮行为**：有滚动条时滚轮滚动标签；内容不足、没有滚动条时滚轮缩放画布。
- **画师下拉框打开时避免瞬间跳动**：选项定位只调整浮层自身的滚动位置，不再联动页面/画布滚动；鼠标点击也不再触发瞬时焦点跳变。

## [4.4.6] - 2026-08-30

### 修复

- **原图标签卡滚轮恢复内部滚动**：原图标签正文现在会把滚轮交给自身滚动，不再被画布缩放逻辑抢走；滚动条和触控板惯性滚动均可正常使用。
- **高级参数卡继续缩放画布**：滚轮行为仅在原图标签正文区域内切换为列表滚动，高级参数卡及其他附加区域仍按画布缩放处理。

## [4.4.5] - 2026-08-30

### 修复

- **附加卡片支持中键平移画布**：高级参数卡和原图标签卡不再拦截中键拖动，用户可以在这些区域按住鼠标中键移动画布。
- **自定义下拉框点击状态优化**：鼠标点击画师等下拉框时不再出现瞬时的焦点光晕，键盘操作仍保留可访问的焦点提示。

## [4.4.4] - 2026-08-30

### 修复

- **提示词卡片附加区域的滚轮行为修正**：高级参数卡和原图标签卡不再吞掉滚轮事件。选中提示词卡片后，鼠标位于这些区域时会继续缩放画布；只有提示词/备注正文编辑区保留滚动文本的行为。

## [4.4.3] - 2026-08-30

### 变更

- **操作记录聚焦重要操作**：界面只展示项目、节点、生成/反推、素材管理、导入导出等关键动作，编辑提示词、平移缩放画布等高频操作不再干扰记录查看。
- **操作记录标题简化**：移除标题右侧的记录数量计数。
- **画幅选项信息顺序调整**：下拉框改为先显示像素尺寸（如 `832×1216`），再显示比例（`2:3`），并恢复四个字段内容的左对齐。

### 修复

- **自定义下拉浮层滚动条统一**：滚动条改为与素材库一致的细窄、圆角、悬停变深样式，避免与整体界面不协调。

## [4.4.2] - 2026-08-30

### 变更

- **四个下拉框改为自定义浮层**：画幅、画师、模型、张数不再依赖浏览器原生选项列表。浮层统一使用画布卡片风格，显示当前选中项、悬停高亮和勾选图标，并根据视口空间自动选择向上或向下展开。
- **下拉框补齐交互保护**：支持鼠标点击与键盘上下/Home/End/Enter 操作，Escape、点击外部、窗口失焦、画布平移缩放或节点重建时会自动收起，避免浮层漂移或残留。
- **说明气泡延迟调整为约 1 秒**：收起仍然立即，减少鼠标扫过卡片时频繁弹出的干扰。

### 修复

- **下拉框选择后立即显示新值**：修复自定义按钮在回调或重绘前仍显示旧选项的问题。
- **“原始提示词”角标样式统一**：与画师预设角标共用同一套几何与视觉样式，仅保留语义类名用于区分。

## [4.4.1] - 2026-08-30

### 变更

- **高级参数不再跟随上一张卡片**。这套跟随把值写进 `meta.steps` 这一层，而生效优先级是 `meta.steps` > `meta.retagSteps` > 默认值 —— 跟随值会**永久盖住**反推读到的原图参数，表现为「明明读到了原图 28 步，滑条却停在上一张卡片的 20」。4.4.0 补的重绘是必要的，但只解决了「不刷新」，盖不住这一层。现在新建提示词卡片的高级参数一律留空，按 原图参数 → 默认值（步数 28 / 引导 7 / Rescale 0，Variety+ 关闭）回落。画幅、画师、模型的跟随不受影响。
  - 相关的 `advSteps` / `advScale` / `advCfgRescale` / `advVariety` 偏好已从前后端整体移除，不再读写。
  - **已存在的卡片不会自动清理**：`meta.steps` 无法区分「跟随写入的」和「你手动拖的」，自动清会误删真正的手动设置。旧卡片如果参数不对，点该项的 ↺ 即可回落。
- **说明气泡改为悬停约 2 秒才浮现**（原先是立即）。鼠标扫过卡片时不该一路弹气泡 —— 这些说明是「想不起来再看一眼」的东西。收起仍然是立即的，延迟只加在浮现那一侧。

### 修复

- **下拉框的自定义箭头改用纯 CSS 绘制**。4.4.0 用的是 `mask` + SVG data URI，解析失败时会退化成一个灰色小方块，等于没优化甚至更糟。现改为两条边框转 45° 画 V 形，零外部依赖，颜色照常跟随主题变量。

## [4.4.0] - 2026-08-30

### 新增

- **原始提示词出图带角标**：raw 生成的图片右上角显示「原始提示词」角标，与画师预设角标同一位置、同一套几何（两者互斥——raw 不追加画师，服务端也就不回 artist），换配色以便区分。图片节点此前根本不记录 raw，现已随工作区落盘，刷新不丢。

### 修复

- **反推读到的原图参数现在会立刻反映到滑条上**：`retagFromNode` 的成功分支只调用了 `scheduleSave()`，唯独漏掉重绘——函数开头和错误分支都有。参数确实写进了节点（所以调试栏看得到），但高级参数卡不重建，滑条一直停在旧值。自动反推是 fire-and-forget，没有调用方兜底重绘，因此「连上图片自动反推」的场景必然撞到。

### 变更

- **操作记录只显示最近 9 条**（原 100 条）。存储上限 240 条保持不变，不影响界面呈现，排查问题时仍可用。
- **说明气泡全部统一**：高级参数卡此前用原生 `title`（悬停约一秒才出来、外观是系统样式），提示词卡用 CSS 气泡（悬停秒开）。现已统一到同一套 `[data-tooltip]` 规则，任何元素挂上即可获得一致的外观与触发时机。
  - 高级参数卡的 body 解除了滚动裁剪 —— 基类的 `overflow-y: auto` 会把向上弹出的气泡整个切掉，而这张卡内容固定，本就不需要滚动（需要滚动的是原图标签卡）。
  - 滑条说明改挂在标题文字上而非整个字段：字段包着 ↺ 按钮，两层都带气泡会同时弹出两个。
  - 三列滑条、Variety+、「原始提示词」等贴边元素按位置分别左对齐/右对齐/居中，确保气泡都落在卡片内。
- **四个下拉框（画幅 / 画师 / 模型 / 张数）不再是浏览器默认样式**：去掉原生箭头，改用跟随主题变量的自定义箭头，补齐 hover / focus 过渡与卡片圆角配色。
  - 已知局限：展开后的选项列表由操作系统绘制，无法样式化，也不随画布缩放。

## [4.3.2] - 2026-08-30

### 修复

- **反推带回的原图参数按滑条上限截取**：原图 50 步 / 引导 15 此前被原样收下，于是滑条卡在上限 28、数字标签写 50、「已沿用原图参数」和调试栏也写 50，而实际发出去的是后端 `_clamp_steps` 钳过的 28 —— 四处口径互相矛盾。现在前端按与后端（`MAX_STEPS` / `MAX_SCALE` / cfg_rescale）完全相同的范围截取，滑条、数字标签、提示行、载荷、调试栏全部一致。
- **超范围的处理从「丢弃」改为「截取」**：此前超出范围会被当成「没这个值」，进而回落到默认值 —— 原图引导 15 会变回默认 7，而不是落到上限 10。现在截取到边界。缺失值仍返回 0，「未设置」的语义不变。
- **落盘上限一并收紧**：只改前端不够，旧存档里已经存下的超限值读回来仍会造成同样的不一致。下限保持 0（那是「未设置」的哨兵值，不能钳成 1）。

## [4.3.1] - 2026-08-30

### 修复

- **高级参数现在真正跟随被反推的图片**：此前把「prompt 是否来自内嵌元数据」当成了采样参数的开关，导致 prompt 命中画布缓存（画布里生成的图再连回去反推）时，图片本身携带的步数 / 引导 / Rescale / 噪声计划 / 采样器被整批丢弃成 0。采样参数直接来自对图片文件的解析，与 prompt 来源无关，现已改为直接采纳。走视觉模型反推的图片本就没有这些参数，行为不变。
- **图标与按钮渲染变慢**：lucide 扫描的是 `[data-lucide]`，而它生成的 SVG 自身也带这个属性，于是每次 `createIcons()` 都会把已经转换好的图标原地重建一遍；节点层又是整层销毁重建，同一个图标在一次渲染里要解析几十次。现在首次转换后缓存成品 SVG，后续直接克隆，并摘掉克隆体的 `data-lucide` 使其不再被重复扫描。
- **「翻译」开关的说明气泡不再被卡片截断**：该开关被 `space-between` 推到卡片中段，气泡沿用左对齐会向右顶出卡片。改为以自身为中心展开并收窄到 200px，卡片最窄（220px）时两侧仍有余量。
- **「原始提示词」的说明文案**：不再声称「按原始英文 NAI tags 生成」—— 4.3.0 起中文可经由「翻译」子开关译出，文案已补上这一点。

## [4.3.0] - 2026-08-30

### 新增

- **原始提示词模式可单独放开翻译**：画布节点勾选「原始提示词」后会多出一个「翻译」子开关。开启后中文描述仍会翻译成 NAI tags，但画师预设和质量词依旧不追加 —— raw 的其余语义完全不变。此前 raw 会连带把中文翻译一起关掉，对习惯写中文描述的用户来说等于 raw 不可用。
  - 该开关仅对画布节点有效，`/nai0` 与 `/nai50` 指令的行为不变，仍然完全不翻译。
  - 反推出来的原图标签**不参与**翻译：翻译只作用于手写提示词，反推标签在翻译完成后才拼接。
- **沿用原图采样器**：反推命中原图内嵌参数时，采样器（`sampler`）也会一并沿用。NAI 元数据里一直带着这个字段，此前解析完就丢弃了，导致「沿用原图参数」独独漏掉采样器。
- **「已沿用原图参数」提示行**：高级参数卡顶部会写明本次沿用了哪些原图参数。步数 / 引导 / Rescale 有滑条可以看出来，但采样器和噪声计划没有对应控件，不写出来无法判断反推是否真的把参数带过来了。

### 修复

- **原图标签的单条移除不再随刷新复活**：在原图标签卡里划掉的标签此前只存在于内存中，刷新画布后全部复活。现在会随工作区落盘，并按 casefold 去重、限制条数与长度。
- **断开原图连接时清空残留参数**：`retagSampler` 与 `retagDropTags` 此前不会随连接断开而清除，导致上一张图的采样器和划掉的标签会静默作用到下一次生成。
- **生成状态文案与实际行为一致**：同时开启「原始提示词」和「翻译」时，界面此前显示「正在生成图片」，而后台其实在等翻译接口返回。前端判断条件现已与后端 `not raw_mode or raw_translate` 对齐。

## [4.2.0] - 2026-08-30

- NovelAI 官方接口直连
- 原图标签支持单条移除
- 修复挂载卡宽度与 `/nai5` 画师预设

## [4.1.0] - 2026-08-30

- 支持 NovelAI 官方接口（Token 直连）
- 安全负面词改由敏感词开关控制
- 修复 `/nai5` 画师预设失效

## [4.0.1] - 2026-08-28

- 退出大图预览不再连带收起素材库

## [4.0.0] - 2026-08-28

- 本地词库、角色站位、元数据与网关字段修正

## [3.9.9] - 2026-08-28

- 修正 Variety+ 字段名：网关方言是 `variety_boost`，不是 `skip_cfg_above_sigma`
- 修正角色名后缀，此前提示词规则写错导致部分角色静默崩掉

## [3.9.8] - 2026-08-28

- 接入本地 Danbooru 词库：校验、别名归一、中文直查、离线兜底
- 新增 SD⇄NAI 权重语法互转，修复 SD 图片参数复用时权重丢失
- 支持 alpha LSB 隐写元数据（`stealth_pngcomp`）
- 非法尺寸改为等比钳制，不再回落到预设比例

## [3.9.7] - 2026-08-27

- V5 提示词方法论、WebP/JPEG 元数据解析、镜像站故障转移

## [3.9.6] - 2026-08-26

- CFG Rescale 与种子处理

## [3.9.5] - 2026-08-25

- 大图预览几何与超时调整

## [3.9.4] - 2026-08-25

- 原始提示词模式与角色识别修复
- 中文翻译策略全模型统一（V5 不再直通），raw 只影响画师串与质量词

## 更早版本

3.9.4 之前的变更请查阅 [提交历史](https://github.com/Menkelo/astrbot_plugin_bestnai_x/commits/main)。项目起始于 2026-06-30。
